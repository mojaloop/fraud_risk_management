version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  predicate-builder-service:
    lang: dockerfile
    handler: .
    image: localhost:5000/predicate-builder-service:latest
    annotations:
      com.openfaas.health.http.path: /

