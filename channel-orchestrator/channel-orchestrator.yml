version: 1.0
provider:
  name: openfaas
  gateway: http://20.49.140.12:8080
functions:
  off-channel-orchestrator:
    lang: dockerfile
    handler: .
    image: frmacr.azurecr.io/off-channel-orchestrator:latest
    # annotations:
    #   com.openfaas.health.http.path: /online
    #   com.openfaas.health.http.initialDelay: "30s"
    secrets:
      - frmpullsecret
    nodeSelector:
        agentpool: generic